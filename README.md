# Moved → [`../supplychain_pulse`](../supplychain_pulse)

Canonical project is **SupplyChainPulse** (`../supplychain_pulse`).

This `procureiq` folder is a leftover shell from earlier renames — safe to delete after you reopen the workspace.
